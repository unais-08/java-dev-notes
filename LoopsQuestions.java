// Write a Java program that takes a number as input and prints its multiplication table up to 10.

// import java.util.Scanner;

public class LoopsQuestions {
    public static void main(String[] args) {
        /*
         * System.out.println("Enter the number you want to print table");
         * Scanner sc = new Scanner(System.in);
         * int number = sc.nextInt();
         * int count = 0;
         * while (count != 10) {
         * count++;
         * int result = number*count;
         * System.out.println(number + "X" + count +"="+result);
         * }
         * int result= count*number;
         * System.out.println(number + "X" + count +"="+result);
         * }while(count!=10);
         * for (int i = 1; i <= 10; i++) {
         * int result = i * number;
         * System.out.println(number + "X" + i + "=" + result);
         * }
         */

        // Question 1: Write a Java program to print all the even numbers from 1 to 100
        // using a for loop.
        /*
         * for (int i = 0; i <= 100; i++) {
         * 
         * if(i%2==0)
         * System.out.println(i);
         * 
         * }
         */
        // Create a program that calculates the factorial of a given number using a
        // while loop.
        // int factorial = 5;
        // while (condition) {

        // }

        // Question 3: Implement a program that sums all the integers from 1 to 100
        // using a do-while loop.
        // int count = 0;
        // int sum = 0;
        // do {
        // count++;
        // sum += count;
        // } while (count != 100);
        // System.out.println(sum);

    }
}