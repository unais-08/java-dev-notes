- Riddhi dutta says use LinkedList from Queue interface in order to solve dsa problem based on normal queue(FIFO)

- Use Stack class itself for dsa problems (to avoid overhead)

- We can implement/use ArrayDeque from Deque interface inorder to solve problem of queue/stack both

- priortyQueue implements Queue interface

- Set interface has hashSet ,LinkedHashSet and TreeSet
